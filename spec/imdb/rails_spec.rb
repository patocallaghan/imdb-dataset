require "spec_helper"

RSpec.describe Imdb::Rails do
  it "has a version number" do
    expect(Imdb::Rails::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
